package com.todolist.demo.dataloader;

/*
@Component
public class DataLoader implements ApplicationListener<ContextRefreshedEvent> {
    private TodoItemService todoItemService;

    public DataLoader(TodoItemService todoItemService) {
        this.todoItemService = todoItemService;
    }

    @Override
    public void onApplicationEvent(ContextRefreshedEvent contextRefreshedEvent) {
        initData();
    }

    private void initData() {
        TodoItem taskName = new TodoItem();
        taskName.setItemId(1L);
        taskName.setTaskName("apple");
        todoItemService.createOrUpdateTodoItem(taskName);
    }

}
 */
